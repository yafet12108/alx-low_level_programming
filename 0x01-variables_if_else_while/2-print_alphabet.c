#include <stuio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int letter 0;

while (letter < 6)
{
putchar('a' + letter);
letter++;	
}
putchar("\n");
return (0);
}
